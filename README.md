# LoRaPayloadSimulator
To test your application server, it's convenient to use a simulator instead of sending more expensive/slow LoRa payload everytime. Here are some tools to help you sent this payload.
